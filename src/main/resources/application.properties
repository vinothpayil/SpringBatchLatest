# Database Configuration
spring.application.name=SpringBatchLatest
spring.datasource.url=jdbc:mysql://localhost:3306/spring_batch_db?allowPublicKeyRetrieval=true&useSSL=false
spring.datasource.username=vinothuser
spring.datasource.password=Vinoth1@3

# JPA Hibernate Properties
spring.jpa.hibernate.ddl-auto=create-drop
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.show-sql=true
spring.jpa.database=mysql
spring.jpa.properties.hibernate.dialect = org.hibernate.dialect.MySQL8Dialect

#Spring Batch Properties
spring.batch.job.enabled=false
spring.batch.jdbc.initialize-schema=always


# server port for the application
server.port=8080

